[module.websocket]
# path = /usr/local/lib/%s/modules/websocket/ # default kosongkan untuk ambil default.
module = "websocket.so"

[[routes]]
endpoint = "/ws"
handler = "WsHandler" # harus diawali huruf besar.

